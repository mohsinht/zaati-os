# Private snapshots

This directory is ignored by Git. Place local snapshots here only for development. In production, use a private data repository or another user-controlled storage adapter.

Never remove the matching `.gitignore` rule in a public fork.
